import React, { useState } from 'react';
import Calendar from 'react-calendar';
const Calendar_component=()=> {

    return (
      <div>
        <Calendar className="my_cal"/>
      </div>
    );
  }
 export default Calendar_component;